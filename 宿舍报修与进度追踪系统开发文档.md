# 宿舍报修与进度追踪系统开发文档

> 本文档基于前后端分离架构（Vue + Python Flask + MySQL）编写，涵盖系统详细功能规格、4 人小组分工方案以及各成员的 AI 辅助开发提示词，供小组开发过程中统一参考。

---

## 1. 系统详细功能规格说明书

本系统为校园宿舍报修平台，主要服务于**学生、维修人员、管理员**三类核心用户，实现报修提交、工单派发、维修进度追踪与数据统计的全流程闭环管理。

### 1.1 学生端（报修模块）

面向学生用户，提供报修提交、进度查询与评价反馈等功能。

#### 1.1.1 首页与报修入口

- **快速报修按钮**：醒目的"一键报修"入口，点击进入报修表单页。
- **我的报修列表**：展示当前学生提交的所有工单，按状态（待处理/处理中/已完成）分类筛选。
- **公告栏**：展示系统公告，如维修时间安排、停水停电通知等。

#### 1.1.2 报修提交页

- **报修表单**：包含以下字段：
  - 报修类型（下拉选择）：水电维修、家具损坏、门窗问题、网络故障、其他
  - 楼栋号与房间号（下拉 + 输入）
  - 故障描述（文本域，限500字）
  - 图片上传（支持拍照/相册，最多3张）
  - 预约时段（选择期望维修时间段）
- **提交确认**：提交前弹出确认对话框，提交后返回工单号。

#### 1.1.3 工单详情与进度追踪

- **工单状态时间线**：以时间线（Timeline）形式展示工单流转节点：
  - 已提交 → 已派单 → 维修中 → 已完成 → 已评价
- **实时状态更新**：展示当前处理人、预计完成时间、维修备注等信息。
- **评价与反馈**：维修完成后，学生对维修速度、维修质量进行星级评分（1-5星）及文字评价。

#### 1.1.4 个人中心

- 个人资料管理（姓名、学号、联系方式、宿舍号）。
- 报修历史记录与统计（总报修次数、平均评分）。

### 1.2 维修人员端（工单处理模块）

面向维修人员，提供工单接收、进度更新与维修记录等功能。

#### 1.2.1 工单工作台

- **待接单列表**：展示已派发至当前维修人员的待处理工单，按紧急程度和提交时间排序。
- **进行中工单**：展示当前正在处理的工单列表。
- **已完成工单**：历史完成记录查看。

#### 1.2.2 工单处理

- **接单/拒单**：维修人员确认接单或拒绝（需填写拒单原因）。
- **进度更新**：维修人员在处理过程中更新状态：
  - 开始维修：填写维修方案、预计用时
  - 维修完成：填写维修结果、使用材料、上传维修后照片
- **紧急工单标记**：对紧急工单进行高亮标记，优先处理。

#### 1.2.3 维修人员个人中心

- 个人信息维护（工种、联系方式）。
- 工作量统计（本月接单数、完成率、平均评分）。

### 1.3 管理端（运营与监控模块）

面向系统管理员，提供用户管理、工单调度与数据分析等功能。

#### 1.3.1 用户与权限管理

- 管理学生、维修人员账号。
- 账号启用/禁用。
- 维修人员工种与区域分配。

#### 1.3.2 工单调度中心

- **工单池**：查看所有新建工单，手动分配至指定维修人员。
- **自动派单规则配置**：按报修类型自动匹配对应工种的维修人员。
- **工单转派**：将工单转给其他维修人员处理。

#### 1.3.3 公告管理

- 发布/编辑/删除系统公告。
- 支持设置公告有效期。

#### 1.3.4 数据统计报表

- **报修统计看板**：
  - 各类报修数量占比（饼图）
  - 每日报修趋势（折线图）
  - 各楼栋报修热度排行（柱状图）
- **维修效率分析**：
  - 平均响应时间、平均完成时间
  - 维修人员工单完成率与评分排行
- **报表导出**：支持导出 Excel 格式的月度/季度统计报表。

### 1.4 非功能性需求

#### 1.4.1 响应性能

页面加载时间控制在 **2 秒以内**，API 接口响应时间控制在 **500ms 以内**。

#### 1.4.2 并发支持

系统支撑 **200 用户同时在线访问**，满足校园场景日常使用。

#### 1.4.3 数据安全

- 用户密码加密存储（bcrypt）。
- 接口采用 JWT Token 鉴权。
- MySQL 数据库每日自动备份。

#### 1.4.4 兼容性

前端页面兼容主流浏览器（Chrome、Firefox、Edge），适配 PC 端与移动端浏览器。

---

## 2. 技术架构设计

### 2.1 整体架构

```
┌─────────────┐     HTTP/HTTPS     ┌─────────────┐     SQL     ┌─────────┐
│   Vue 前端   │ ──────────────────→ │  Flask 后端  │ ──────────→ │  MySQL  │
│  (端口 5173)  │ ←────────────────── │  (端口 5000)  │ ←────────── │ (3306)  │
└─────────────┘     JSON/REST       └─────────────┘            └─────────┘
```

### 2.2 技术选型

| 层级 | 技术 | 说明 |
|------|------|------|
| 前端 | Vue 3 + Vue Router + Pinia | 组合式 API，路由与状态管理 |
| UI 组件库 | Element Plus | PC 端组件库，开箱即用 |
| 后端 | Python 3 + Flask | 轻量级 Web 框架，开发快 |
| 数据库 | MySQL 8.0 | 关系型数据库，满足课设要求 |
| ORM | Flask-SQLAlchemy | Python 操作 MySQL 的 ORM 工具 |
| 认证 | PyJWT | 生成与验证 JWT Token |
| 文件上传 | Flask-Upload / 本地存储 | 报修图片存储 |
| 接口规范 | RESTful API | 前后端数据交互 |

### 2.3 数据库表结构设计

#### 2.3.1 用户表（user）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT (PK, AUTO_INCREMENT) | 用户ID |
| username | VARCHAR(50) UNIQUE | 用户名/学号 |
| password | VARCHAR(255) | 密码（bcrypt加密） |
| role | ENUM('student','worker','admin') | 角色 |
| name | VARCHAR(50) | 真实姓名 |
| phone | VARCHAR(20) | 联系方式 |
| building | VARCHAR(20) | 楼栋号（学生） |
| room | VARCHAR(20) | 房间号（学生） |
| specialty | VARCHAR(50) | 工种（维修人员） |
| status | TINYINT DEFAULT 1 | 账号状态（1启用/0禁用） |
| created_at | DATETIME | 创建时间 |

#### 2.3.2 工单表（repair_order）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT (PK, AUTO_INCREMENT) | 工单ID |
| order_no | VARCHAR(30) UNIQUE | 工单编号（如 WX20260915001） |
| student_id | INT (FK→user.id) | 报修学生ID |
| worker_id | INT (FK→user.id) NULL | 指派维修人员ID |
| category | ENUM('water','electric','furniture','door','network','other') | 报修类型 |
| building | VARCHAR(20) | 楼栋号 |
| room | VARCHAR(20) | 房间号 |
| description | TEXT | 故障描述 |
| appointment | VARCHAR(50) | 预约时段 |
| status | ENUM('submitted','assigned','repairing','completed','rated') | 工单状态 |
| created_at | DATETIME | 提交时间 |
| assigned_at | DATETIME NULL | 派单时间 |
| completed_at | DATETIME NULL | 完成时间 |

#### 2.3.3 工单图片表（order_image）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT (PK, AUTO_INCREMENT) | 图片ID |
| order_id | INT (FK→repair_order.id) | 关联工单ID |
| image_path | VARCHAR(255) | 图片存储路径 |
| type | ENUM('report','repair') | 报修图/维修后图 |

#### 2.3.4 工单进度记录表（order_progress）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT (PK, AUTO_INCREMENT) | 记录ID |
| order_id | INT (FK→repair_order.id) | 关联工单ID |
| status | VARCHAR(20) | 状态变更值 |
| operator_id | INT (FK→user.id) | 操作人ID |
| remark | TEXT NULL | 备注/说明 |
| created_at | DATETIME | 操作时间 |

#### 2.3.5 评价表（rating）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT (PK, AUTO_INCREMENT) | 评价ID |
| order_id | INT (FK→repair_order.id) UNIQUE | 关联工单ID |
| student_id | INT (FK→user.id) | 评价学生ID |
| worker_id | INT (FK→user.id) | 被评价维修人员ID |
| speed_score | TINYINT | 响应速度评分（1-5） |
| quality_score | TINYINT | 维修质量评分（1-5） |
| comment | TEXT NULL | 文字评价 |
| created_at | DATETIME | 评价时间 |

#### 2.3.6 公告表（announcement）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT (PK, AUTO_INCREMENT) | 公告ID |
| title | VARCHAR(100) | 公告标题 |
| content | TEXT | 公告内容 |
| expire_at | DATETIME NULL | 过期时间 |
| created_by | INT (FK→user.id) | 发布人ID |
| created_at | DATETIME | 发布时间 |

---

## 3. RESTful API 接口设计

### 3.1 认证接口

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/auth/register | 用户注册 |
| POST | /api/auth/login | 用户登录，返回JWT |
| GET | /api/auth/profile | 获取当前用户信息 |
| PUT | /api/auth/profile | 更新当前用户信息 |

### 3.2 工单接口

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/orders | 学生提交报修工单 |
| GET | /api/orders | 获取工单列表（支持分页、筛选） |
| GET | /api/orders/:id | 获取工单详情 |
| PUT | /api/orders/:id/assign | 管理员派单 |
| PUT | /api/orders/:id/accept | 维修人员接单 |
| PUT | /api/orders/:id/progress | 维修人员更新进度 |
| PUT | /api/orders/:id/complete | 维修人员标记完成 |
| POST | /api/orders/:id/rate | 学生评价 |

### 3.3 管理接口

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/admin/users | 获取用户列表 |
| PUT | /api/admin/users/:id/status | 启用/禁用用户 |
| GET | /api/admin/statistics | 获取统计数据 |
| GET | /api/admin/statistics/export | 导出报表 |

### 3.4 公告接口

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/announcements | 获取公告列表 |
| POST | /api/announcements | 发布公告（管理员） |
| PUT | /api/announcements/:id | 编辑公告 |
| DELETE | /api/announcements/:id | 删除公告 |

---

## 4. 4 人小组详细分工方案

按"统筹 + 前端 + 后端 + 测试"的方式进行分工，职责明确、协作高效。

### 4.1 分工总览

| 成员 | 角色定位 | 核心负责模块 | 交付成果物 |
|------|---------|-------------|-----------|
| 同学 A | 项目统筹 / 产品经理 | 需求分析、数据库设计、API文档编写、项目管理与进度把控、部署运维、文档撰写 | 需求文档、API接口文档、数据库建表SQL、部署配置、用户手册、项目总结 |
| 同学 B | 后端开发 | Flask 后端全部开发：项目搭建、数据模型、所有业务接口、JWT 鉴权、文件上传 | Flask 后端完整代码（32个接口） |
| 同学 C | 前端开发 | Vue 前端全部开发：项目搭建、所有页面（学生端/维修端/管理端）、公共组件、接口联调 | Vue 前端完整代码（全部页面与组件） |
| 同学 D | 测试工程师 | 测试用例设计、接口测试、前端功能测试、自动化测试、性能测试、Bug跟踪、验收报告 | 测试用例、Postman测试集、pytest单元测试、验收测试报告 |

### 4.2 同学 A：项目统筹 / 产品经理

- **核心职责**：
  1. 需求分析与需求文档编写。
  2. 数据库表结构设计（6张表）。
  3. API 接口文档编写，供 B 开发、C 对接、D 测试使用。
  4. 页面原型图绘制，供 C 参考开发。
  5. 项目进度跟踪、任务分配、Bug 分配与跟踪。
  6. 部署配置（Nginx + Gunicorn）。
  7. 用户手册与项目总结文档撰写。
- **交付成果物**：需求文档、API接口文档、数据库建表SQL、页面原型图、部署配置、用户手册、项目总结。

### 4.3 同学 B：后端开发

- **核心职责**：
  1. 搭建 Flask 项目骨架，配置数据库连接。
  2. 实现全部数据模型（6张表映射）。
  3. 实现用户认证模块（注册、登录、JWT 生成与验证）。
  4. 实现全部 REST API 接口（工单 CRUD、派单、进度更新、评价、公告、统计、报表导出，共32个接口）。
  5. 实现文件上传、工单编号生成等工具函数。
- **交付成果物**：Flask 后端完整代码（32个接口、数据模型、中间件、工具函数）。

### 4.4 同学 C：前端开发

- **核心职责**：
  1. Vue 项目搭建与基础建设（路由、状态管理、请求封装）。
  2. 登录/注册页（三端共用）。
  3. 学生端页面（首页、报修提交、工单详情、个人中心）。
  4. 维修人员端页面（工作台、工单处理、个人中心）。
  5. 管理端页面（用户管理、工单调度、公告管理、数据统计看板、维修人员管理）。
  6. 公共组件开发（工单卡片、状态时间线、评价弹窗、图片上传等）。
  7. 与 B 联调全部接口。
- **交付成果物**：Vue 前端完整代码（全部页面、组件、接口封装、联调通过）。

### 4.5 同学 D：测试工程师

- **核心职责**：
  1. 测试计划与测试用例设计（认证模块、工单模块、管理模块、前端页面）。
  2. Postman 接口测试（覆盖全部32个接口，含正常/异常参数）。
  3. 前端功能测试（各端页面交互、跨端完整流程验证）。
  4. pytest 自动化单元测试（认证、工单流程、权限控制）。
  5. 性能测试（接口响应时间、并发测试）。
  6. 安全测试（SQL注入、XSS、未授权访问检查）。
  7. Bug 跟踪（GitHub Issues）与回归测试。
  8. 验收测试报告编写。
- **交付成果物**：测试用例、Postman测试集、pytest单元测试、性能测试结果、安全测试清单、Bug跟踪记录、验收测试报告。

---

## 5. 各成员详细 AI 提示词

### 5.1 同学 A（项目统筹）专用提示词

**Prompt：**

> 请帮我为"宿舍报修与进度追踪系统"设计数据库和编写API接口文档，要求：
> 1. 设计6张MySQL数据表：用户表（user，含角色student/worker/admin）、工单表（repair_order，含状态submitted/assigned/repairing/completed/rated）、工单图片表（order_image）、工单进度记录表（order_progress）、评价表（rating）、公告表（announcement），给出完整的建表SQL（含索引、外键、注释）。
> 2. 按RESTful规范编写全部API接口文档，涵盖：认证接口（注册/登录/个人信息）、工单接口（提交/列表/详情/派单/接单/拒单/进度更新/完成/评价/图片上传）、管理接口（用户管理/工单调度/公告CRUD/统计/报表导出），每个接口列出URL、方法、请求参数、响应格式。
> 3. 提供一份数据库初始化脚本，插入测试数据（3个角色用户、示例工单、公告等）。

### 5.2 同学 B（后端开发）专用提示词

**Prompt：**

> 请帮我用 Python Flask 框架开发"宿舍报修与进度追踪系统"的后端，要求：
> 1. 使用 Flask-SQLAlchemy ORM 设计以下数据表：用户表（user，含角色 student/worker/admin）、工单表（repair_order，含状态 submitted/assigned/repairing/completed/rated）、工单图片表（order_image）、工单进度记录表（order_progress）、评价表（rating）、公告表（announcement）。
> 2. 实现用户注册/登录接口，使用 bcrypt 加密密码、PyJWT 生成 Token。
> 3. 实现工单的提交、列表查询（支持分页和按状态筛选）、详情查看、派单、接单、拒单、进度更新、完成、评价等完整接口。
> 4. 实现管理端的用户管理、公告管理和统计接口（各类报修数量、每日趋势、楼栋排行、维修效率）及Excel报表导出接口。
> 5. 实现文件上传工具（图片存储、类型校验）和工单编号生成器（格式WX20260915001）。
> 6. 代码结构规范，使用 Blueprint 组织路由，提供完整 Flask 项目代码。

### 5.3 同学 C（前端开发）专用提示词

**Prompt：**

> 请帮我用 Vue 3 + Element Plus + ECharts 开发"宿舍报修与进度追踪系统"的全部前端页面，要求：
> 1. 登录/注册页：用户名+密码登录，支持角色选择（学生/维修人员/管理员），注册含对应角色字段。
> 2. 学生端：首页（一键报修按钮+我的报修列表+公告栏）、报修提交页（类型下拉+楼栋房间+描述+图片上传+预约时段）、工单详情页（状态时间线el-timeline+评价弹窗）、个人中心。
> 3. 维修人员端：工作台（Tabs三栏：待接单/进行中/已完成）、工单处理页（接单/拒单+维修方案+照片上传+完成标记）、个人中心（工作量统计）。
> 4. 管理端：用户管理页（el-table+启用/禁用）、工单调度中心（工单池+手动派单+转派）、公告管理页（CRUD+el-dialog）、数据统计看板（ECharts饼图+折线图+柱状图+Excel导出）、维修人员管理。
> 5. 使用 Pinia 管理状态，axios 封装 API 请求（自动携带 JWT Token），Vue Router 含路由守卫和角色权限控制，提供完整 Vue 组件代码。

### 5.4 同学 D（测试工程师）专用提示词

**Prompt：**

> 请帮我为"宿舍报修与进度追踪系统"设计测试方案，要求：
> 1. 编写功能测试用例，覆盖认证模块（注册/登录/Token验证）、工单模块（提交报修/状态流转/派单/接单/拒单/完成/评价）、管理模块（用户管理/工单调度/公告CRUD/统计/导出）、前端页面（表单校验/页面跳转/交互逻辑），用等价类划分和边界值方法设计。
> 2. 设计Postman接口测试集合，覆盖全部接口的正常/异常参数场景（缺字段/越权/重复操作/边界值）。
> 3. 编写pytest单元测试代码，覆盖：认证流程、工单完整流转（提交→派单→接单→完成→评价）、权限控制（越权访问校验）。
> 4. 列出安全测试检查项（SQL注入、XSS、未授权访问、Token伪造、密码明文存储）。
> 5. 提供性能测试方案（验证API响应时间<500ms，50并发提交报修无数据错乱）。

---

## 6. 项目目录结构

### 6.1 后端（Python Flask）

```
backend/
├── app/
│   ├── __init__.py          # Flask应用工厂
│   ├── config.py            # 配置文件（数据库连接等）
│   ├── models/              # 数据模型
│   │   ├── __init__.py
│   │   ├── user.py
│   │   ├── repair_order.py
│   │   ├── order_image.py
│   │   ├── order_progress.py
│   │   ├── rating.py
│   │   └── announcement.py
│   ├── blueprints/          # 路由蓝图
│   │   ├── __init__.py
│   │   ├── auth.py          # 认证接口
│   │   ├── orders.py        # 工单接口
│   │   ├── admin.py         # 管理接口
│   │   └── announcements.py # 公告接口
│   ├── utils/               # 工具函数
│   │   ├── auth_helper.py   # JWT工具
│   │   └── response.py      # 统一响应格式
│   └── static/              # 上传文件存储
├── migrations/              # 数据库迁移
├── requirements.txt         # 依赖清单
└── run.py                   # 启动入口
```

### 6.2 前端（Vue 3）

```
frontend/
├── public/
├── src/
│   ├── api/                 # API请求封装
│   │   ├── auth.js
│   │   ├── orders.js
│   │   ├── admin.js
│   │   └── announcements.js
│   ├── assets/              # 静态资源
│   ├── components/          # 公共组件
│   │   ├── OrderCard.vue    # 工单卡片
│   │   ├── StatusTimeline.vue # 状态时间线
│   │   └── RatingDialog.vue # 评价弹窗
│   ├── layouts/             # 布局组件
│   │   ├── StudentLayout.vue
│   │   ├── WorkerLayout.vue
│   │   └── AdminLayout.vue
│   ├── router/              # 路由配置
│   │   └── index.js
│   ├── stores/              # Pinia状态管理
│   │   └── user.js
│   ├── views/               # 页面组件
│   │   ├── student/
│   │   │   ├── HomeView.vue
│   │   │   ├── SubmitRepair.vue
│   │   │   ├── OrderDetail.vue
│   │   │   └── StudentProfile.vue
│   │   ├── worker/
│   │   │   ├── WorkerDashboard.vue
│   │   │   ├── HandleOrder.vue
│   │   │   └── WorkerProfile.vue
│   │   └── admin/
│   │       ├── UserManage.vue
│   │       ├── OrderDispatch.vue
│   │       ├── AnnouncementManage.vue
│   │       └── StatisticsBoard.vue
│   ├── App.vue
│   └── main.js
├── package.json
└── vite.config.js
```

---

## 7. 开发里程碑

| 阶段 | 时间 | 里程碑目标 |
|------|------|-----------|
| 第1周 | 第1-7天 | 完成需求确认、数据库建表、后端项目搭建与核心API开发 |
| 第2周 | 第8-14天 | 后端全部API完成并测试通过；前端项目搭建、路由与布局完成 |
| 第3周 | 第15-21天 | 前端三端页面开发完成，与后端API联调 |
| 第4周 | 第22-28天 | 功能测试、Bug修复、文档编写、部署上线 |
